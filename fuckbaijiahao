🧩 方法一：浏览器书签脚本（Bookmarklet）
你可以创建一个书签脚本，在点击时自动将当前 Bing 搜索加上排除词：

javascript
javascript:(function(){
  var q = prompt("请输入搜索关键词：");
  if (q) {
    window.location.href = "https://www.bing.com/search?q=" + encodeURIComponent(q + " -baijiahao.baidu.com");
  }
})();
✅ 使用方法：
在浏览器书签栏创建一个新书签

将上面的代码粘贴到“网址”栏

点击书签 → 输入关键词 → 自动跳转 Bing 搜索并排除百家号

🧠 方法二：自定义搜索引擎（适用于 Chrome）
打开 Chrome 设置 → 搜索引擎 → 管理搜索引擎

添加新搜索引擎：

名称：Bing 排除百家号

关键词：bing-

URL：https://www.bing.com/search?q=%s+-baijiahao.baidu.com

使用方法：地址栏输入 bing- 你的关键词，即可自动搜索并排除百家号

🧪 方法三：简单 Python 脚本（适合编程用户）
python
import webbrowser

def bing_search(query):
    exclude = "-baijiahao.baidu.com"
    full_query = f"{query} {exclude}"
    url = f"https://www.bing.com/search?q={full_query}"
    webbrowser.open(url)

# 示例调用
bing_search("人工智能趋势")
✅ 使用方法：
保存为 .py 文件并运行

输入关键词后自动打开 Bing 搜索页面并排除百家号
